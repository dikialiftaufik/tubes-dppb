name: tubes
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  http: ^1.2.0
  shared_preferences: ^2.2.2 # Untuk menyimpan token login
  intl: ^0.19.0 # Untuk format tanggal notifikasi

  # Typography
  google_fonts: ^6.1.0
  image_picker: ^1.0.7
  
  # Asset handling
  flutter_svg: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - lib/assets/
